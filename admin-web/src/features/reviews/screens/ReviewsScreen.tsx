// ReviewsScreen — moderación de reseñas. Stub por ahora.
// PR futuro: listado con filtros (rating, reportadas), acciones (eliminar, flagear).

import { Page } from '@/shared/components/Page/Page';

export function ReviewsScreen() {
  return (
    <Page
      title="Reseñas"
      subtitle="Moderación de reseñas. Próximamente: listado ordenable y acciones de moderación."
    />
  );
}
